from pathlib import Path


root = Path(__file__).parent.parent.parent
data = root / 'data'
docs = root / 'docs'
figs = root / 'figs'
models = root / 'models'
notebooks = root / 'notebooks'
src = root / 'src'
